# OCR-Task
This project is an Optical Character Recognition (OCR) application that extracts product information from images using image processing and text recognition techniques. The system scans product labels or packaging images and converts the visual text into machine-readable format.
